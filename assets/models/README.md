# 将你的 .glb 模型文件放在这里

请将从 Blender 导出的 `.glb` 文件放在此文件夹中。

## 使用方法

1. 将你的模型文件重命名为 `your-model.glb`
2. 或者修改 `index.html` 中的路径来匹配你的文件名

例如，如果你的文件名是 `my-awesome-model.glb`，请在 `index.html` 中修改：

```html
<model-viewer src="assets/models/my-awesome-model.glb" ...>
```
